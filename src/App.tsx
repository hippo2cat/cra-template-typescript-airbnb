import { FC } from 'react';
import styles from './App.module.less';

const App: FC = () => (
  <div className={styles.app}>
    <div>cra-template</div>
    <div>containes</div>
    <div>typescript/eslint(typescript, airbnb)/less/customize-cra/proxy support</div>
  </div>
);

export default App;
